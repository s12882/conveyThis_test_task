; Matches docker/nginx/default.conf's client_max_body_size — all three
; layers (nginx, this, and config('files.max_size_kb')/FILE_MAX_SIZE_KB)
; need to move together when the upload limit is changed. See README.md.
upload_max_filesize = 100M
post_max_size = 105M
memory_limit = 256M
